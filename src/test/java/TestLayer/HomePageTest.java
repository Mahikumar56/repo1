package TestLayer;

import org.testng.Assert;
import org.testng.annotations.Test;

import BaseLayer.BaseClass;

/*
 * public class HomePageTest extends BaseClass {
 * 
 * @Test(priority = 2) public void validateHomePageTitle() { String actualTitle
 * = driver.getTitle(); Assert.assertEquals(actualTitle, "Dashboard");
 * 
 * }
 * 
 * @Test(priority = 3) public void validateHomePageUrl() { String actualUrl =
 * driver.getCurrentUrl(); Assert.assertTrue(actualUrl.contains("HMIS")); } }
 */
